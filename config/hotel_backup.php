<?php

/**
 * إعدادات النسخ الاحتياطي السحابي (أمر hotel:backup). مصمَّم لخادم محلي يعمل
 * داخل شبكة الفندق بلا إنترنت يومياً — الإنترنت يُستخدم فقط لحظة رفع النسخة.
 */
return [

    // قرص التخزين السحابي (من config/filesystems.php) — s3_private موجود مسبقاً
    // ويعمل مع أي تخزين متوافق مع S3 (AWS S3, Backblaze B2, Cloudflare R2...).
    // اضبط بيانات AWS_* في .env حسب مزوّد التخزين الذي تختاره.
    'disk' => env('BACKUP_DISK', 's3_private'),

    // المسار داخل القرص السحابي حيث تُخزَّن ملفات النسخ الاحتياطي
    'path' => env('BACKUP_PATH', 'backups'),

    // عدد النسخ المحفوظة سحابياً — يُحذف الأقدم تلقائياً عند تجاوز هذا العدد
    'keep' => (int) env('BACKUP_KEEP', 30),

    // تضمين ملفات النزلاء والسندات (هويات، صور أضرار، سندات تحويل) في كل نسخة
    // افتراضياً false لأن حجمها يكبر مع الوقت — يمكن تفعيلها للنسخة الأسبوعية مثلاً
    'include_files' => filter_var(env('BACKUP_INCLUDE_FILES', false), FILTER_VALIDATE_BOOLEAN),

];
