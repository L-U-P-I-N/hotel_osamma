# تشغيل النظام أوفلاين + نسخ احتياطي تلقائي للسحابة

هذه السكربتات تجعل النظام يعمل بشكل كامل بدون إنترنت على جهاز الفندق، وترفع
نسخة احتياطية تلقائياً إلى OneDrive/Google Drive كلما توفر الإنترنت.

## التثبيت على جهاز فارغ تماماً (الطريقة الأسهل — ملف واحد فقط)

`install_hotel_system.exe` هو نفس السكربت أعلاه بعد تحويله لملف تنفيذي عادي.
لا يحتاج العميل يعرف أي شيء عن PowerShell أو الأوامر — فقط:

1. حمّل الملف من هذا الرابط مباشرة على جهاز الفندق:
   ```
   https://raw.githubusercontent.com/L-U-P-I-N/hotel_osamma/main/deploy/install_hotel_system.exe
   ```
2. **دبل كليك** على الملف المُحمَّل
3. لو ظهرت رسالة زرقاء من ويندوز "Windows protected your PC" (لأن الملف غير
   موقَّع رقمياً بشهادة مدفوعة) — اضغط **"More info"** ثم **"Run anyway"**
4. لو ظهرت نافذة "هل تريد السماح لهذا التطبيق..." (UAC) — اضغط **"Yes"**
5. انتظر حتى تظهر رسالة خضراء "Installation complete!" — هذا كل شيء

الملف يقوم بكل شيء من الصفر تلقائياً: تثبيت Git وNode.js وLaragon (PHP+Apache)،
تحميل المشروع كاملاً من GitHub، تثبيت كل المكتبات، إعداد قاعدة البيانات،
تسجيل الموقع كخدمة Windows دائمة، وإعداد اختصار سطح المكتب والنسخ الاحتياطي
التلقائي.

**كل خطوة تُسجَّل في `hotel_install_log.txt` على سطح المكتب** — لو توقف عند
أي نقطة، أرسل هذا الملف لتشخيص المشكلة بالضبط. السكربت آمن لإعادة التشغيل.

### الطريقة البديلة (لمن يفضّل PowerShell)

نفس الأداة بالضبط لكن كسكربت `.ps1` بدل `.exe` — `install_fresh_machine.ps1`
في هذا المجلد:
1. افتح PowerShell **كمسؤول (Run as Administrator)**
2. نزّل المشروع أولاً (git clone أو zip من GitHub) إلى `C:\laragon\www\hotel_osamma`
3. نفّذ:
   ```powershell
   powershell -ExecutionPolicy Bypass -File C:\laragon\www\hotel_osamma\deploy\install_fresh_machine.ps1
   ```
4. انتظر حتى تظهر رسالة خضراء "Installation complete!"

### تحديث ملف الـ exe بعد أي تعديل على install_fresh_machine.ps1

```powershell
Install-Module -Name ps2exe -Scope CurrentUser -Force -AllowClobber
Import-Module ps2exe
Invoke-ps2exe `
    -inputFile "install_fresh_machine.ps1" `
    -outputFile "install_hotel_system.exe" `
    -title "Hotel Osamma - Installer" `
    -requireAdmin
```

**مهم جداً:**
- السكربت **آمن لإعادة التشغيل** — لو توقف أو فشل في أي خطوة، شغّله من جديد
  وسيكمل من حيث توقف بدل ما يبدأ من الصفر.
- كل خطوة تُسجَّل في ملف `hotel_install_log.txt` على سطح المكتب. لو ظهرت
  رسالة حمراء "FAILED"، أرسل هذا الملف لمعرفة سبب المشكلة بالضبط.
- إذا كان فيه نسخة احتياطية (ملف zip فيه `database-*.sql`)، ضعه في سطح
  المكتب أو مجلد التنزيلات قبل تشغيل السكربت — سيكتشفه، ويعرض لك اسم
  الملف وتاريخ تنزيله وكم مضى عليه، **ويطلب تأكيدك (y/n) قبل الاستيراد**
  لتفادي استيراد نسخة قديمة بالغلط. بعد الاستيراد يطبع تواريخ آخر حجز/دفعة
  فعلية بالبيانات — تأكد أنها قريبة من لحظة التحويل قبل ما تعتبر العملية تمّت.
- **لم يُختبر هذا السكربت بشكل كامل من الألف للياء على جهاز فارغ حقيقي** —
  كل أمر بداخله مُختبر منفرداً ونجح، لكن التسلسل الكامل معاً يحتاج أول تشغيل
  حقيقي ليُتأكَّد 100%. يُفضَّل مراقبة أول تشغيل إن أمكن.

## الفكرة

- التطبيق يعمل محلياً 24 ساعة عبر Apache كخدمة Windows دائمة (`HotelOsammaApache`)
  ولا يحتاج إنترنت إطلاقاً للعمل اليومي.
- كل ساعة، مهمة مجدولة تأخذ لقطة آمنة من قاعدة البيانات (لا تتأثر حتى لو كان
  أحد يستخدم النظام في نفس اللحظة) وتضعها في مجلد OneDrive/Google Drive.
- برنامج OneDrive/Google Drive نفسه يتكفّل برفعها للسحابة تلقائياً بمجرد
  توفر الإنترنت — لا حاجة لأي منطق "كشف اتصال" مخصص، وهذا أكثر أماناً وموثوقية.
- النسخة الأونلاين تبقى **نسخة احتياطية فقط** ولا يُكتب عليها مباشرة، فلا يوجد
  خطر تعارض بيانات.

## الملفات

| الملف | الوظيفة |
|---|---|
| `backup_snapshot.php` | يأخذ لقطة متسقة وآمنة من `database.sqlite` باستخدام أمر `VACUUM INTO` (لا يفسد البيانات حتى أثناء الكتابة عليها) |
| `backup_to_cloud.ps1` | ينسّق عملية النسخ الكامل: قاعدة البيانات + مجلد المرفقات (`storage/app/private`) → ملف zip بالتاريخ والوقت → مجلد OneDrive/Google Drive تلقائياً. يحتفظ بآخر 20 نسخة فقط ويحذف الأقدم |
| `install_scheduled_backup.ps1` | **شغّله مرة واحدة فقط** بعد تثبيت المشروع على جهاز العميل — يسجّل مهمة Windows مجدولة تُشغّل `backup_to_cloud.ps1` كل ساعة تلقائياً |
| `create_desktop_shortcut.ps1` | **شغّله مرة واحدة فقط** — ينشئ اختصار "نظام الفندق" على سطح مكتب العميل يفتح `http://localhost` مباشرة في المتصفح. هذا كل ما يحتاج العميل الضغط عليه |

## خطوات التثبيت على جهاز العميل (مرة واحدة فقط)

بعد اتباع دليل التثبيت الأساسي (PHP 8.3 + Laragon + git clone + composer +
npm + migrate + استيراد البيانات + خدمة Apache)، نفّذ من داخل PowerShell:

```powershell
cd C:\laragon\www\hotel_osamma\deploy
powershell -ExecutionPolicy Bypass -File create_desktop_shortcut.ps1
powershell -ExecutionPolicy Bypass -File install_scheduled_backup.ps1
```

بعدها لا يحتاج العميل فعل أي شيء — فقط الضغط على اختصار "نظام الفندق" على
سطح المكتب، والنظام يعمل والنسخ الاحتياطي يتم تلقائياً في الخلفية.

## ملاحظة مهمة

يجب أن يكون OneDrive (المدمج مع ويندوز) أو Google Drive مثبّتاً ومسجَّلاً
دخول على جهاز العميل قبل تشغيل `install_scheduled_backup.ps1`، وإلا ستُحفظ
النسخ محلياً فقط في `backups_local_only` داخل مجلد المشروع دون رفعها للسحابة.
